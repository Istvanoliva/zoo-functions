// const { species } = require('../data/zoo_data');
// const data = require('../data/zoo_data');

// const animalsNE = species.filter((animal) => animal.location === 'NE');
// const animalsNW = species.filter((animal) => animal.location === 'NW');
// const animalsSE = species.filter((animal) => animal.location === 'SE');
// const animalsSW = species.filter((animal) => animal.location === 'SW');

// function noParam () {
//   return species.filter((specie) => specie.location)
// }

// function getAnimalMap(options) {
//   if (!options) { return noParam }

// }

// console.log(noParam())
// module.exports = getAnimalMap;

// console.log(animalsNE);
